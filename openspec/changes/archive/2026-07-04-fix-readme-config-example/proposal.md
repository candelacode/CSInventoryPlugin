## Why

The README shows a full bot config JSON example with `SteamLogin` and `SteamPassword`, which is misleading — users might think they need to include those in their config when only `sendcsitems` is relevant to the plugin.

## What Changes

- Replace the full bot config JSON block with a minimal example showing just `"sendcsitems": true`

## Capabilities

### New Capabilities

None — documentation only.

### Modified Capabilities

None — no behavior changes.

## Impact

- `README.md`: one JSON code block updated
